// Média

// Crie um código que receba as 3 notas de um aluno e mostre a média dessas notas.
// ===========================

var trabalhoDeHistoria = 8,
    trabalhoDeMatematica = 7,
    trabalhoDeCiencia = 10,
    quantidadeDeTrabalhos = 3;

var media = (
        trabalhoDeHistoria + 
        trabalhoDeMatematica + 
        trabalhoDeCiencia ) / quantidadeDeTrabalhos;

console.log(media);